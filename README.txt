Before installing the file there is dependent software that needs to be installed. 

MySQL
Express
EJS
NodeJS

On the zip folder there are two .sql files.

The create_db.sql  file creates the database, this must be entered into mysql directly. 
The insert_test_data.sql file will populate the database with data. 

You can start the website by typing in node index.js. 

The node modules won’t be included in the zip files. These can be installed on the virtual server or other linux command line environment. 
